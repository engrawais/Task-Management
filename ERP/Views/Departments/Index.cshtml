@model IEnumerable<ERP.Models.TM_Department>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.DepName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.DepName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TDepID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TDepID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TDepID })
        </td>
    </tr>
}

</table>
