@model IEnumerable<ERP.Models.TM_Projects>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ProName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ProName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TProID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TProID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TProID })
        </td>
    </tr>
}

</table>
